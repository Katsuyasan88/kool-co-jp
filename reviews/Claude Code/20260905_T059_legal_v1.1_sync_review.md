# T-059 SmartThanksCorporate 文書版1.1 同期 自己レビュー（ガチャちょう 利用規約・プライバシーポリシー）

- 作成日: 2026-09-05
- 対象タスク: CapCole `T-059` のうち、ユーザー承認済み文書版1.1をコーポレートサイトへ同期する作業
- 正本: `C:\Users\user\AIProjects\CapCole\reviews\Codex\20260904_T059_review.md` 「第14回進行確認（2026-09-05）」、CapCole `site/gachacho/legal/versions/1.1.json`（CapCole HEAD `047508c`）
- ブランチ: `feature/gachacho-privacy-support`（分岐元 `05c5de3`）
- 状態: ローカル実装・build・検証完了、commit / push 済み。**本番 deploy は未実施**（差分レビューとユーザーの個別承認待ち）
- 前回レビュー: `reviews/Claude Code/20260904_T059_smartthankscorporate_review.md`

## 1. 実施内容

| # | 指示 | 結果 |
|---|---|---|
| 1 | `/gachacho/legal/current.json` を文書版1.1へ | CapCole `versions/1.1.json` をそのままコピーして差し替え（本文の書き換えなし） |
| 2 | 版別 `1.1` の追加 | `public/gachacho/legal/versions/1.1.json` を新規配置（同じファイルのコピー） |
| 3 | `/gachacho/terms` | 変更なし。`current.json` を取得して描画する既存実装のまま、文書版1.1の本文・メタ行を表示することを確認 |
| 4 | `/privacy#gachacho` | 変更なし。同じ `current.json` の `privacy` 本文を描画し、文書版1.1を表示することを確認 |
| 5 | 既存版 1.0 / 1.0.1 の維持 | `versions/1.0.json`、`versions/1.0.1.json` は内容変更なし（git blob 同一）。配信も確認 |
| 6 | 旧 `/terms` リダイレクトの維持 | `src/App.tsx` / `LegacyTermsRedirect.tsx` は変更なし。動作確認済み |

### 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `public/gachacho/legal/current.json` | 文書版1.0.1 → 1.1（CapCole 1.1.json と同一） |
| `public/gachacho/legal/versions/1.1.json` | 新規（CapCole 1.1.json と同一） |
| `.gitattributes` | 新規。`public/gachacho/legal/**/*.json` を `text eol=lf` に固定。`core.autocrlf=true` 環境の checkout で法務JSONが CRLF に変換され、CapCole 生成物と working copy / `dist` のバイト列が変わることを防ぐ（git blob は元から LF で変更なし） |
| `docs/DEPLOYMENT.md` | 5.3 公開後確認の例示版を `1.0.1` → `1.1` に更新（2行） |
| `public/sitemap.xml` | `/gachacho/terms`、`/privacy` の `lastmod` を `2026-09-05` に更新 |

### 触っていないもの

- CapCole リポジトリ（読み取りとファイルコピーのみ。`git status` で CapCole 側に本作業による変更なし）
- `src/` 配下（描画ロジック・ルート・リダイレクトは既存のまま）
- `infrastructure/`、AWS（S3 / CloudFront / Route 53）、Firebase、Google / Apple のストア設定
- 未追跡の `.claude/`、`reviews/Codex/`（commit に含めていない）

## 2. 本文・版・日付・hash の一致

| 項目 | CapCole 正本 | SmartThanksCorporate | 判定 |
|---|---|---|---|
| `documentVersion` | `1.1` | `1.1`（`current.json`、`versions/1.1.json`） | 一致 |
| `effectiveDate` | `2026-08-31` | `2026-08-31` | 一致 |
| `lastUpdated` | `2026-09-04` | `2026-09-04` | 一致 |
| SHA-256（`versions/1.1.json`） | `e05c6d46…317b5` | `e05c6d46…317b5` | 一致 |
| SHA-256（`current.json`） | `e05c6d46…317b5`（CapCole `current.json` も 1.1） | `e05c6d46…317b5` | 一致 |
| git blob（`1.1.json` / `current.json`） | `9d493e5c…` | `9d493e5c…` | 一致 |
| git blob（`versions/1.0.1.json`） | `4263a7dc…` | `4263a7dc…` | 一致（変更なし） |
| git blob（`versions/1.0.json`） | `6085b52c…` | `6085b52c…` | 一致（変更なし） |
| `dist/gachacho/legal/*.json` | — | `current.json` = `versions/1.1.json` = `e05c6d46…`、`1.0.1` = `7530473e…`、`1.0` = `5dfecdfe…` | build 出力も正本と一致 |

SHA-256 の全文: `e05c6d462af4324d4e5ba0fadc2eda0714517d705b7cf1b29de221f4bfa317b5`（1.1）、`7530473e0a11a9ab10681d7887a31398d88b2105963213e19050a5597d345c2a`（1.0.1）、`5dfecdfe2fb24df2a77ffced349bc1ac762d5eab52df6c5f815e81929bd17062`（1.0）。

### 1.0.1 → 1.1 の本文差分（見出し単位・参考）

本文はコピーであり、このリポジトリで編集していない。差分の把握のため、見出し単位で比較した結果のみ記す。

- 利用規約（14条 → 14条）: 第4条「匿名アカウント」→「アカウントと任意連携」に置換、第5条「利用者が送信する画像・情報」を改訂。他の条は同一
- プライバシーポリシー（11項 → 11項）: 「1. 取得・取り扱う情報」「2. 利用目的」「3. 外部サービスの利用」「5. 保存場所・保存期間」「6. 利用者による削除」を改訂。lead・他の項は同一
- 本文中の URL は `https://smartthanks.world/gachacho/terms`、`/privacy`、`/contact` の3つ、連絡先メールは `support_smartthanks@kool.co.jp` のみ（すべて公開URL・公開連絡先）

## 3. 検証結果（ローカル）

| 項目 | 結果 |
|---|---|
| `npm run lint` / `npm run build` / `git diff --check` | すべて成功 |
| `/gachacho/terms` | title「ガチャちょう 利用規約」、h1「利用規約」、メタ行「文書版 1.1 ／ 制定日 2026年8月31日 ／ 最終更新日 2026年9月4日」、h2 14 件（第4条は「アカウントと任意連携」）、canonical `https://smartthanks.world/gachacho/terms`。機械可読版リンクは `current.json` と `versions/1.1.json` |
| `/gachacho/terms` 本文一致 | JSON の `terms` 全 59 単位（見出し・段落・箇条書き）が描画 DOM に欠落なく含まれる（`missing: []`） |
| `/privacy#gachacho` | メタ行「プライバシーポリシー（アプリ「ガチャちょう」）／ 文書版 1.1 ／ 制定日 2026年8月31日 ／ 最終更新日 2026年9月4日」、h3 11 件、表 1、箇条書き 6、直接アクセスで第9項へスクロール（要素上端 112px）。リンクは `/gachacho/terms`、`current.json`、`versions/1.1.json`、`/contact`、`mailto:` のみ |
| `/privacy#gachacho` 本文一致 | JSON の `privacy` 全 79 単位（lead 含む）が DOM に欠落なく含まれる（`missing: []`） |
| 旧 `/terms` | `/terms?x=1#top` → `/gachacho/terms?x=1#top` に置換遷移し、文書版1.1の h2 14 件を描画 |
| 法務JSON配信 | `current.json`（1.1）、`versions/1.1.json`（1.1）、`versions/1.0.1.json`（1.0.1）、`versions/1.0.json`（1.0）がすべて 200 / JSON。存在しない版はSPAフォールバック（本番は CloudFront の既存挙動に従う） |
| 内部注記非混入 | `1.1.json` を `TODO / FIXME / 内部 / Codex / Claude / T-059 / レビュー / 注記 / draft / 未確定` で grep。本文語（「思い出メモ」「仮」を含む語）以外の一致なし。描画 DOM でも `Codex / T-059 / TODO / 内部注記` 不一致 |
| 主要幅 | 375px で `/gachacho/terms`、`/privacy#gachacho` とも横スクロールなし（scrollWidth = 375）。コンソールエラーなし |
| CapCole 側 | 変更なし（本作業前から存在する `docs/*.md`・Codex レビューmdの未commit差分は触っていない） |

補足: ブラウザ検証ツールのスクリーンショットは `/gachacho/terms` 先頭では取得できたが、`/privacy` を第9項までスクロールした状態では白紙画像しか返らなかった（DOM 上は `opacity: 1`・`visibility: visible`・`elementFromPoint` が第9項）。ツール側の取得不良と判断し、本文一致は DOM 検証を根拠とした。`/privacy` の描画コードは本作業で変更していない。

## 4. 公開後確認（第三者が判定できる期待値）

`docs/DEPLOYMENT.md` 5.3 に従う。deploy 後の期待値:

```bash
curl -s https://smartthanks.world/gachacho/legal/current.json | python -c "import sys,json; print(json.load(sys.stdin)['documentVersion'])"
```

- 出力が `1.1`
- `curl -sI https://smartthanks.world/gachacho/legal/versions/1.1.json` が `200` / `application/json; charset=utf-8`
- `versions/1.0.json`、`versions/1.0.1.json` が引き続き `200`
- `https://smartthanks.world/terms` が `/gachacho/terms` へ遷移し、文書版 1.1 を表示

## 5. 未実施（承認待ち）

- SmartThanksCorporate の本番 deploy（`deploy.ps1`）と `apply-legal-cors.ps1 -Apply`
- App Privacy / Data safety の保存、Firebase / AWS の production backend・Rules・provider、Apple 秘密鍵入力、version / build 変更、Archive / AAB、ストアアップロード・提出・公開
- 法務専門家によるレビュー（本レビューは正本との一致と配信・描画の検証であり、法的妥当性の判定ではない）

## 6. 停止位置

commit / push 済み。差分レビュー（Codex）とユーザーの deploy 承認を待つ。
