# 実装担当へ渡す指示文

SmartThanksCorporateの既存 `/gachacho` LPを改修してください。今回の目的は、掲載画像をユーザー指定の1.0.3版3枚へ更新し、LP全体を「生成りの紙・深緑・セージ・くすんだ青」を基調にした、落ち着いたネイチャー調に整えることです。

作業リポジトリ：`C:/Users/user/AIProjects/SmartThanksCorporate`

受け渡し一式：`C:/Users/user/AIProjects/CapCole/design/exploration/v1.0.3/lp-nature-handoff/`

最初に作業リポジトリのブランチと `git status --short` を確認し、既存差分を保護してください。適用される領域指示と `CLAUDE.md`、受け渡し一式の `LP_REVISION_SPEC.md`、`ASSET_MANIFEST.json`、`assets/original/` の4枚を読んでから着手してください。`docs/GACHACHO_LP_TASK.md` は初回実装時の背景・維持すべき導線を知るための参照資料です。その古い作業指示を今回の依頼として実行しないでください。

以下を実装してください。

1. `src/pages/Gachacho.tsx` の既存LPを更新します。ルート新設やLPの作り直し用依存ライブラリの追加は不要です。
2. `assets/web/` の3枚を `public/gachacho/` に取り込み、既存の `STEPS` の画像を01→02→03の順に差し替えます。仕様書の `srcSet`・寸法を使用してください。3枚目の上下逆向きの端末は「向かい合って見せ合う」意図のある完成素材です。回転を直したり、スマホ枠をさらに重ねたり、画像内の文字やピンクを加工したりしないでください。
3. 4枚目はLPの配色・余白・紙／手帳／植物／カフェの素材感の参考です。そこから生成りと緑・青をLPの基調へ展開してください。文字入り参考画像を全面背景にして、その上へHTMLの見出しを重ねないでください。画像を新規生成する作業は今回の実装を成立させる必須条件ではありません。
4. Hero見出しを「集めて、見せ合う。」、副見出しを「シール帳みたいな、ガチャちょう。」にします。本文・使い方・記録欄は仕様書の掲載原稿に揃えます。「個人利用だけ」「ひとりで管理するだけ」という説明へ狭めず、友だちとお互いのアプリを開いて見せ合う価値を伝えてください。
5. ピンクのぼかし背景・色付きの強い影・LP要素の大きな傾きを整理します。深緑の見出し、生成りの余白、セージの小面積、細い罫線、控えめな青を使ってください。3枚の掲載画像のピンクは素材内に残します。
6. `tailwind.config.js` にLP用の色・見出しフォントを定義し、LPから参照してください。既存 `gachacho` 色の利用箇所を検索し、`gachacho-nature` 名前空間の追加などで影響をLPに限定します。コーポレート全体のprimary・Navbar・Footer・法務ページの色は変更しません。
7. LPのアプリアイコンを同梱の新しい手帳アイコンに更新します。`/service` のガチャちょう紹介だけは同じアイコンへ揃えて構いません。その他の事業紹介は維持してください。
8. App Storeの公式バッジと `https://apps.apple.com/jp/app/id6798359468`、運営者、問い合わせ、利用規約、プライバシーへの導線を維持します。Androidの公開状況は公開前に確認し、一般公開が未確認なら「Android版は準備中です。」を維持してください。

320 / 390 / 768 / 1024 / 1440pxで確認し、特に320pxで画像・文言・タップ領域が切れないようにしてください。文字200%、キーボード操作、動きを減らす設定でも確認します。主要情報をカルーセルや開閉UIへ隠す必要はありません。

`npm run lint`、`npm run build`、`git diff --check` と仕様書の画面・リンク確認を行い、実施した結果を記録してください。ローカル画面のスクショ、変更ファイル、素材対応、未確認事項、公開時の対象とrollbackを `reviews/Claude Code/20260909_T072_gachacho_lp_nature_review.md`（既存なら追記）へまとめてください。プレビューをユーザーに見せられる状態まで仕上げてください。

今回の到達点はローカル実装・検証・レビュー提出です。CapColeのアプリ／backend／法務本文、公開JSON、ストア管理画面、OAuth設定は変更しません。旧文書にある自動commit/pushや本番deployも実行しません。1.0.3公開状況とサイト公開の判断は、完成した差分を確認した後の工程としてレビューに残してください。
